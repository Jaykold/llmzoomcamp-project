apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    orgId: 1
    uid: postgres
    url: postgres:5432
    user: postgres
    secureJsonData:
      password: postgres
    jsonData:
      database: llm_project
      sslmode: disable
      postgresVersion: 1400
      timescaledb: false
    isDefault: true
    version: 1
    editable: true
